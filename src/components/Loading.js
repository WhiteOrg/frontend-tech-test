import React from "react";
import { LoaderAndErrorWrapper, Loader } from "../styles/styles";

const Loading = () => (
  <LoaderAndErrorWrapper>
    <Loader />
  </LoaderAndErrorWrapper>
);

export default Loading;
